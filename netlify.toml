[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Site 100% estático: index.html + arquivos .json versionados no repo.
# Alterações da área do gestor passam por Netlify Functions.
